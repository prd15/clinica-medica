# Copie este arquivo para .env e preencha com os valores reais
DB_USER=
DB_PASS=

# Segredo do client tecnico Keycloak (Feign interno)
# Valor padrao de demo: clinica-service-secret (definido no realm-clinica.json)
KEYCLOAK_SERVICE_SECRET=clinica-service-secret
