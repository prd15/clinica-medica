server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: administrativo
          uri: ${ADMINISTRATIVO_URL:http://localhost:8081}
          predicates:
            - Path=/api/admin/**
          filters:
            - StripPrefix=2

        - id: agendamento
          uri: ${AGENDAMENTO_URL:http://localhost:8082}
          predicates:
            - Path=/api/agendamentos/**
          filters:
            - StripPrefix=2

        - id: atendimento
          uri: ${ATENDIMENTO_URL:http://localhost:8083}
          predicates:
            - Path=/api/atendimentos/**
          filters:
            - StripPrefix=2

  security:
    oauth2:
      resourceserver:
        jwt:
          # jwk-set-uri: lazy fetch das chaves publicas; nao valida issuer automaticamente
          # Dentro do Docker: KEYCLOAK_JWK_URI=http://keycloak:8180/realms/clinica/protocol/openid-connect/certs
          jwk-set-uri: ${KEYCLOAK_JWK_URI:http://localhost:8180/realms/clinica/protocol/openid-connect/certs}

management:
  endpoints:
    web:
      exposure:
        include: health,info
